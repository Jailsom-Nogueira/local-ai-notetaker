# Continue rules for Notetaker

Follow `AGENTS.md` as the canonical instructions.

Prioritize:
- local-first privacy,
- server-only secrets,
- strict TypeScript,
- explicit upload/id validation,
- transcript-as-untrusted-data prompt safety,
- publication hygiene via `npm run check:publication`,
- full verification with `npm run check` before completion.
